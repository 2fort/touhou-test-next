import { style } from 'typestyle';

export const reverseTest = style({
  background: '#f9f9f9',
  borderRadius: '2px',
  boxShadow: '0 1px 4px 0 rgba(0, 0, 0, .14)',
  display: 'flex',
  flexDirection: 'row',
  flexWrap: 'nowrap',
  margin: '0 auto',
  maxWidth: '80rem',
  padding: 0,
});

export const testCenter = style({
  flex: '1 1 90%',
});

export const characterName = style({
  margin: '1rem auto',
  textAlign: 'center',
});
